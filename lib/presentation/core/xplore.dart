import 'package:flutter/material.dart';

class XploreApp extends StatelessWidget {
  const XploreApp({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}